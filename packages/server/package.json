{
  "name": "server",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "tscad-server": "server/server.ts"
  },
  "scripts": {
    "dev": "vite",
    "format": "bun x prettier -w .",
    "prebuild": "bun run format",
    "build": "tsc && vite build",
    "start": "bun server/server.ts models"
  },
  "dependencies": {
    "occ-tscad": "workspace:*"
  }
}
