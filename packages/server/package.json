{
  "name": "server",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "format": "bun x prettier -w .",
    "prebuild": "bun run format",
    "build": "tsc"
  },
  "dependencies": {
    "occ-tscad": "workspace:*"
  }
}
