{
  "name": "occ-tscad-static",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "format": "bun x prettier -w .",
    "prebuild": "bun run format",
    "build": "tsc && vite build",
    "postbuild": "bun bin/postbuild.ts"
  },
  "dependencies": {
    "occ-tscad": "workspace:*",
    "occ-tscad-base": "workspace:*"
  }
}
